---

- path: "/run/systemd/network/eth0.network"
  type: "file"
  data: |
    [Match]
    Name=eth0

    [Network]
    Address="{{ .Config.IPv4.Address }}"
    Gateway="{{ .Config.IPv4.Gateway }}"
    Range="{{ range nil }}"
